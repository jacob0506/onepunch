(() => {
  const root = window;
  if (!root.Game) root.Game = {};
  const g = root.Game;
  if (!g.core) g.core = {};
  if (!g.data) g.data = {};
  if (!g.domain) g.domain = {};
  if (!g.battle) g.battle = {};
  if (!g.ui) g.ui = {};
})();

